import React from 'react';

const AnimeState = ({children}) => {

    const addToList = async () => {

    }

    return (
        <div>
            {children}
        </div>
    );
};

export default AnimeState;